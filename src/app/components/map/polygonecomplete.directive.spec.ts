import { PolygonecompleteDirective } from './polygonecomplete.directive';

describe('PolygonecompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new PolygonecompleteDirective();
    expect(directive).toBeTruthy();
  });
});
