import { UniqueRoutePipe } from './unique-route.pipe';

describe('UniqueRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
