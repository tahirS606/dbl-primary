export interface Property {
  id: string;
  name: string;
  address: string;
  route: number; 
  latitude: number;
  longitude: number; 
}
  // route
  // owner (current)
  // contact (email / phone?)

