export interface Property {
  id: string;
  name: string;
  address: string;
  latitude: number;
  longitude: number; }
  // needs map integration
  // route
  // owner (current)
  // contact (email / phone?)

